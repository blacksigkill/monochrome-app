/// iOS-specific setup.
///
/// Future additions: NowPlaying lock screen controls, background audio config,
/// haptic feedback, native share sheet, etc.
pub fn setup(_app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
